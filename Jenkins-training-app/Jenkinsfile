pipeline {
	agent any

    stages {
		stage('whoami')
		{
			steps {
				sh 'whoami'
			}
		}
        
    }

    post {
        success {
            echo 'Build, test, and publish successful!'
        }
    }
}